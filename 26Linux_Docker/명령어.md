*과거 docker 수업 내용 참고 : LLM\20openai\12_1_0_docker_redis.ipynb

### docker를 이용한 ubuntu 설치
- docker 설치 : https://www.docker.com/ ---> 윈도우 AMD64 설치
- powershell - 관리자 권한으로 실행
- `wsl --install`
- user acoount : playdata2 / pw: 입력하기
- 재부팅 
- `docker desktop` 실행해서 docker 실행 
- power shell 이나, 터미널 cmd에서 `docker info` : 정보확인
- power shell 이나, 터미널 cmd에서 `docker run -it ubuntu bash`
    ==> 현재 컨테이너 안에 들어온 상태 (root@8~~ 나옴)
    ==> 이거하고나면, docker.desktop에서 container 에 새로운 것이 생김
    ==> 상기 컨테이너 안에 들어온 상태에서, 각종 명령어 실행


### docker 컨테이너 확인 및 실행
- `docker run -it ubuntu bash` : 도커를 이용해서 ubuntu를 컨테이너에 넣고 실행하고 컨테이너에 진입
- `docker ps -a` : 현재 실행된 및 종료된 컨테이너 목록
- 만약에 중지되어 있으면 `docker desktop으로 실행` 또는 `docker start [CONTAINER ID]`
- `docker exec -it [CONTAINER ID] bash`: 실행중인 컨테이너 안에 진입



### 1. 디렉토리 이동 및 위치 확인
- `pwd`: 현재 위치한 디렉토리의 경로를 출력합니다.
- `cd /`: 최상위(root) 디렉토리로 이동합니다.
- `cd ~`: 현재 사용자의 홈 디렉토리로 이동합니다.
- `cd ..`: 상위 디렉토리로 이동합니다.
- `ls`: 현재 디렉토리의 파일과 폴더 목록을 보여줍니다.
- `ls -al`: 숨겨진 파일을 포함하여 상세 정보를 출력합니다.
- `ls -l` : 디렉토리 목록 
- `ls -R` : 하위디렉토리 까지 다 보여줌
- `ls -d */` : 파일은 제외하고 디렉토리만 골라서 보여줌
- `ls -a` : 숨김파일 까지 보임


### 2. 파일 및 디렉토리 관리
- `mkdir my_folder`: 'my_folder'라는 이름의 새 디렉토리를 만듭니다.
- `touch hello.txt`: 내용이 비어있는 'hello.txt' 파일을 생성합니다.
- `echo "Hello Linux" > hello.txt`: 파일에 텍스트를 입력합니다.
- `cat hello.txt`: 파일의 내용을 터미널에 출력합니다.
- `cp hello.txt copy.txt`: 파일을 복사합니다.
- `mv copy.txt my_folder/`: 파일을 폴더 안으로 이동시킵니다.
- `rm hello.txt`: 파일을 삭제합니다.
- `rm -rf my_folder`: 디렉토리와 그 안의 내용을 모두 강제로 삭제합니다.
- `clear`: 터미널의 출력 내용을 지웁니다.


### 3. 리눅스 디렉토리 구조(FHS)
- 윈도우와 달리 C, D 드라이브 개념이 없고 모든 것이 루트(/)에서 시작하는 단일 구조
    - /(root) : 최상위
    - /bin & /usr/bin : 기본적인 명령어들(ls, cp)이 위치한 곳
    - /etc : 시스템 설정 파일들의 보관소(윈도우의 제어판 + 레지스토리 역할)
    - /home : 사용자의 홈 디렉토리 (일반 사용자들의 개인 디렉토리)
    - /var : 로그 파일, 데이터베이슬 등 수시로 변하는 파일 저장소
    - /tmp : 임시 파일 저장소, 재부팅시 자동삭제가 될수도 있는 파일들


### 4. 목록보기
- `cd /etc`
- `ls` : 디렉토리 목록 그냥 이름만 보기
- `ls -l` : 상세 정보 보기 (권한, 소유자, 크기, 날짜)
- `ls -a` : 숨겨진 파일(.으로 시작)까지 모두 보기
- `ls -al` : 숨겨진 파일을 포함해 상세 정보 보기
- `ls -lh` : 파일 크기를 사람이 읽기 편하게(K, M, G 단위) 표시
- `ls -R` : 하위 디렉토리 내용까지 펼쳐서 보기 (재귀적, 스크롤 주의!)
- `ls -d */` : 파일은 제외하고 디렉토리만 골라서 보기

### 5. 디렉토리 생성
- `mkdir -p project/backend/api` : 하위 지정 디렉토리 생성
- `mkdir project/frontend project/db` : 프로젝트 폴더내에 다른 폴더 생성
- `cp -r backend backend_backup` : 백업폴더 생성
- `mv stylee.css style.css` : 파일명 변경
- `rm -r backend_backup/` : 폴더 삭제

### 6. 파일생성
- `cd project`
- `touch README.md` : 빈 파일 생성
- `touch index.html style.css` : 여러 파일 동시 생성
- `touch archive/image.jpg` : 경로지정해서 파일 생성
- `touch .gitignore` : 숨김 파일 생성
- `ls` : .gitignore 안 보임
- `ls -a` : 이제 보임
- `ls archive/` : 경로 이동하지 않고 해당폴더의 목록 이름 확인 가능

### 7. 복사의 다양한 옵션
- `cp README.md README_bak.md` : 파일 복사
- `cp -r backend backend_backup` : 폴더 통째로 복사할 땐 -r (recursive) 필수!
- `cp -v index.html index_v2.html` : 복사 과정을 자세히 출력 (-v: verbose)

### 8. 이동(mv)과 이름변경
- `mv style.css frontend/`
- `mv index_v2.html old_index.html` : 이름 변경 (같은 폴더 내 이동 = 이름 변경)


### 9. 삭제(rm) - 주의
- `rm old_index.html` : 파일 삭제 (되살릴 수 없음!)
- `rm -r backend_backup` : 폴더 삭제는 -r 필수
- `rm -rf project/db` : 묻지도 따지지도 않고 강제 삭제 (-f: force, 매우 위험)
    - 주의! rm -rf / 명령은 시스템 전체를 날릴 수 있습니다. 절대 금지!
- `rm archive/image.jpg` : 경로 지정해서 파일삭제


### 10. 에디터
- `ls -R /etc > file_list.txt` :  /etc 목록을 파일로 저장 (내용이 꽤 김)
- `cat file_list.txt` : file_list.txt 파일내용 전체 출력
- `head file_list.txt` : 
- `head -n 5 file_list.txt` : 첫 5줄만 보기
- `tail -n 5 file_list.txt` : 마지막 5줄만 보기
- `vi file_list.txt` :
echo "<div><h2>hello work</h2></div>" > index.html

### 11. nano 에디터
- `apt update && apt install nano -y` : 패키지 목록 갱신 후 nano 설치
- `nano my_config.conf` : 나노 에디터 실행
- ServerIP=192.168.0.1 (샘플 입력)
- 복사/붙여넣기 : Alt+6( 복사 ) Ctrl+U (붙여넣기)
- 잘라내기 : Ctrl+k (한줄 삭제/ 잘라내기)
- 검색 : Ctrl+w 누르고 찾을 단어 입력
- 저장 및 종료 : Ctrl+o(Write out) -> Enter -> Ctrl+x(Exit)


### 12. 로그 실시간 감시
- `touch access.log` : 빈 파일 생성
- `tail -f access.log` : 파일에 내용이 추가될 때마다 실시간으로 화면에 출력/ 파워셀창을 2개 띄어두고 한쪽에 echo "hello" > access.log 를 실행하면, `tail -f access.log` 여기를 실행해둔 파워셀창에서 hello가 나오는것을 확인할 수 있음/ `nano access.log` 실행하고 `ctrl+O` `enter키` 하면 `tail -f access.log` 여기를 실행해둔 파워셀창에서 실시간으로 확인 가능함


### 13. Grep&Find
```
샘플 데이터
echo -e "Apple\nBanana\nCherry\nDate\nElderberry" > fruits.txt
echo -e "Error: 404\nInfo: Login success\nError: 500\nWarn: Low disk" > server.log
```
- `grep "Apple" fruits.txt`         # 기본 검색
- `grep -i "apple" fruits.txt`     # 대소문자 무시 (-i)
- `grep -v "Error" server.log`      # "Error"가 *없는* 줄만 출력 (-v: 반전)
- `grep -n "Error" server.log`      # 몇 번째 줄인지 줄 번호 표시 (-n)
- `grep -c "Error" server.log`      # 매칭되는 줄의 개수 카운트 (-c)
- `grep "^Error" server.log`        # "Error"로 *시작하는* 줄만 검색 (정규식 ^)
- `grep find /etc -name '*.conf'` # etc 폴더에서 확장자가 conf인 것들 찾아줌

```
find 명령어
```
- `find /etc -name "*.conf"`        # /etc 아래 확장자가 .conf인 모든 파일 찾기
- `find /usr -size +10M`            # 10MB보다 큰 파일 찾기
- `find . -type d`                  # 현재 경로 아래의 '디렉토리'만 찾기
- `find . -name "fruits.txt" -delete` # 찾아서 바로 삭제 (주의!)

```
파이프 응용
```
- /etc 디렉토리 파일 중 'conf'가 들어가는 파일 개수 세기
    - `ls /etc | grep "conf" | wc -l`

- 프로세스 목록 중 'bash'만 찾아서 보기
    - `ps aux | grep bash`

```
/var 디렉토리 전체에서 이름에 log가 들어가는 파일을 찾으세요.
위에서 찾은 파일 중, 파일 크기가 1kbyte 이상인 것만 찾아보세요. (find /var -name "*log*" -size +1k)
ls -al /etc 명령어의 결과에서 "root"라는 단어가 몇 번 나오는지 파이프와 grep -c를 이용해 한 줄 명령어로 구하세요.
```


```
cat << EOF > grep_practice.txt
Apple Pie
Banana Split
Cherry Cake
apple juice
Banana Bread
Date 2024
Error: File not found
Error: Access denied
Warning: Disk full
Info: Update completed
EOF
```


- grep -i -n -v -c "apple" grep_practice.txt
- grep -i -n -v -c -r
- grep -i -n -v -c -r -l

| 옵션 | 설명 | 기억법 |
| :--- | :--- | :--- |
| `-i` | 대소문자 무시 | **I**gnore case |
| `-v` | 패턴 제외 (반전) | In**V**ert |
| `-n` | 줄 번호 표시 | **N**umber |
| `-r` | 하위 폴더 포함 검색 | **R**ecursive |
| `-c` | 개수 카운트 | **C**ount |
| `-l` | 파일명만 출력 | **L**ist file |
| `^` | 라인의 시작 | (Shift+6) |
| `$` | 라인의 끝 | (Shift+4) |



- `ps aux` : 지금 이 시스템에서 돌아가고 있는 모든 프로세스(프로그램)를 보는 명령어
    - 누가(USER), 어떤 프로그램(COMMAND)을, 얼마나 자원 쓰면서 실행 중인지 보여줌
    - USER   PID  %CPU %MEM  VSZ   RSS  TTY  STAT START   TIME COMMAND
    - USER : 사용자
    - PID : 프로세스 ID (고유 번호)
    - %CPU : CPU 사용률
    - %MEM : 메모리 사용률
    - VSZ : 가상 메모리 크기
    - RSS : 실제 메모리 크기
    - TTY : 터미널
    - STAT : 프로세스 상태 (R,S)
        - R : 실행중
        - S : 대기 중 (정상)
        - D : I/O대기
        - Z : 좀비 프로세스
        - T : 중지됨
    - COMMAND : 실행 중인 명령
    


- 파이프(|)는 앞 명령어의 출력 결과를 뒤 명령어의 입력으로 넘겨주는 연결선이야.
- `ps aux | grep python`
    - ps aux → 실행 중인 모든 프로세스 목록 출력
    - | → 그 결과를
    - grep python → python이 들어간 줄만 골라냄


### 14. 권한
```
실습용 파일 생성
touch secret.txt
ls -l secret.txt
```
- ls -l secret_data.txt
-rw-r--r-- 1 root root 0 Dec 31 06:05 secret_data.txt
-rw-r--r-- 해석
- -파일종류 : - 일반파일 d 디렉토리(폴더) l 심볼릭 링크
- rw- 소유자 r-- 그룹 r-- 기타(other)
- r read, w write, x execute, - 권한 없음
- 소유자는 읽기O, 쓰기O, 실행X // 그룹과 기타는 읽기만 가능
- 숫자로 권한 변경하는 방법
    - r read = 4, w write = 2, x execute = 1
    - `chmod 777 secret_data.txt`  ==> ls -l secret_data.txt ==> -rwxrwxrwx 1 root root 0 Dec 31 06:05 secret_data.txt
- 심볼릭으로 권한 변경하는 방법
    - chmod u+rwx  secret_data.txt
    - chmod u-x secret_data.txt


| 구분 | 권한(User) | 권한(Group) | 권한(Others) | 링크수 | 소유자 | 그룹 | 크기 | 날짜 | 이름 |
|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| `-` | `rw-` | `r--` | `r--` | `1` | `root` | `root` | `0` | ... | `secret.txt` |
*(맨 앞이 `d`면 디렉토리, `-`면 파일입니다)*

2진수 비트 계산(r=4, w=2, x=1)을 사용합니다.

| 권한 조합 | 숫자 | 의미 |
|:---:|:---:|:---|
| `rwx` | 4+2+1 = **7** | 읽고 쓰고 실행 가능 (관리자 권한) |
| `rw-` | 4+2+0 = **6** | 읽고 쓰기 가능 (일반 파일 표준) |
| `r-x` | 4+0+1 = **5** | 읽고 실행 가능 (일반 프로그램/폴더 표준) |
| `r--` | 4+0+0 = **4** | 읽기만 가능 (중요 설정 파일) |
| `---` | 0+0+0 = **0** | 아무것도 못함 (접근 금지) |

#### 권한변경 - 숫자모드
- `chmod 777 secret.txt`  # rwxrwxrwx (누구나 다 가능 - 보안 위험!)
- `ls -l secret.txt`

- `chmod 755 secret.txt`  # rwxr-xr-x (주인은 맘대로, 남들은 실행/읽기만)
- `ls -l secret.txt`

- `chmod 600 secret.txt`  # rw------- (나만 읽고 쓰기 - 개인키/비번 파일 필수)
- `ls -l secret.txt`


#### 권한변경 - 심볼릭모드
- `chmod u+x secret.txt`  # User에게 eXecute 권한 추가
- `chmod g-w secret.txt`  # Group에게 Write 권한 제거
- `chmod o=r secret.txt`  # Others는 Read만 가능하게 고정
- `chmod a-r secret.txt`  # All(모두)에게서 Read 권한 뺏기 (나도 못 읽음!)


### 사용자 변경 
- `useradd -m developer` 실습용 사용자 추가
- `chown developer secret.txt` 파일 소유자를 developer로 변경
- `ls -l secret.txt`       # 소유자가 root -> developer로 바뀜
- `chown root:root secret.txt` # 소유자와 그룹을 동시에 변경 (사용자:그룹)
- `chown :group file` # 그룹만 변경
- ===> 주의! 리눅스는 UID(숫자)로 사용자를 판단하기 때문에 같은 이름이라도 (devloper uid 1000, uid 2000) uid가 다르면 다르게 해석하므로 uid를 맞추는게 중요함

```
실습
mkdir -p /var/www/html
touch /var/www/html/index.html

- 디렉토리는 755 (누구나 들어올 수 있음)
chmod 755 /var/www/html

# 파일은 644 (누구나 읽을 수 있음)
chmod 644 /var/www/html/index.html

# 확인
ls -ld /var/www/html
ls -l /var/www/html/index.html
```

- `ls /home` : 시스템에 존재하는 일반 사용자 계정 목록 보기
- `passwd developer` : developer 계정의 비밀번호 설정 / 변경
- `su developer` 
    - 사용자만 변경
    - 위치(pwd), 환경변수는 root것 유지
- `su -developer` 
    - developer로 새로 로그인
    - 홈 디렉토리 -> /home/developer
    - 환경변수 -> developer기준
    - 권한은 developer 그대로


### 15. 시스템 관리자 와 패키지
패키지 관리 라이브러리 라이프사이클
```
apt update                      # 서버에서 최신 패키지 정보를 가져옴
apt install htop curl tree -y   # htop, curl, tree 설치
apt remove tree                 # tree 삭제 (설정 파일은 남음)
apt purge tree                  # tree 완전 삭제 (설정 파일 포함)
apt autoremove                  # 쓰지 않는 의존성 패키지 자동 정리
```
시스템 상태 확인
```
top -b -n 1                     # 현재 시스템 상태 스냅샷 (CPU, 메모리)
htop                            # 컬러풀한 그래픽 모드 (F10 또는 q로 종료)
df -h                           # 디스크 용량 확인 (Disk Free) -h: Human readable
du -sh /var/log                 # 특정 디렉토리의 용량 확인 (Disk Usage)
free -h                         # 남은 메모리(RAM) 확인
```
프로제스 제어
```
# 백그라운드에서 오래 도는 프로세스 시뮬레이션
sleep 1000 &                    # 1000초 동안 대기하는 명령을 백그라운드(&) 실행
ps aux | grep sleep             # sleep 프로세스 찾기 (PID 확인)
sleep 1000 & ps aux| grep sleep  
-> sleep 1000 : 1000초 동안 아무것도 안 하고 대기하는 프로그램
-> & : 백그라운드 실행
-> ps aux : 현재 실행 중인 모든 프로세스 출력
-> | : 앞 명령의 결과를 뒤 명령의 입력으로 전달
-> grep sleep : sleep 이라는 글자가 들어간 줄만 필터링


# 프로세스 강제 종료 (PID가 1234라면)
kill 1234
kill -9 1234                  # 말을 안 들으면 강제 종료 (-9)
pkill sleep                     # 이름으로 바로 종료
```

```
실습:
ncdu라는 디스크 사용량 분석 도구를 apt로 설치해보세요.
설치된 ncdu를 실행하여 현재 디렉토리(/)의 용량 분포를 확인하고 종료(q)하세요.
현재 실행 중인 프로세스 중 메모리를 가장 많이 사용하는 프로세스 상위 5개를 출력하는 명령어를 찾아보세요. 
(힌트: ps 명령어의 정렬 옵션 또는 top 사용)
```

- top -b -n 1
ps aux | grep sleep
pkill sleep
pkill 3422


### 16. 쉘 스크립트 프로그래밍(Shell Scripting)
```
Shebang (#!/bin/bash): 스크립트 첫 줄에 필수. 어떤 쉘로 실행할지 지정.
```

nano hello.sh
```
#!/bin/bash
NAME="LinuxUser"              # 변수 지정 (공백 없어야 함)
echo "Hello, $NAME!"
echo "오늘 날짜는 $(date) 입니다." # 명령어 결과 넣기 $()

echo -n "당신의 이름은? "
read USER_INPUT               # 사용자 입력 받기
echo "반갑습니다, $USER_INPUT 님."
```
chmod +x hello.sh

./hello.sh

#### 17. 조건문(check_file.sh)
```
nano check_file.sh

#!/bin/bash
FILE='mylog.txt'
if [ -f "$FILE" ]; then
        echo "$FILE is exist"
        cat $FILE
else
        echo "$FILE is not exist, and create that"
        touch $FILE
fi
-> nano 저장 후 나와서

chmod +x check_file.sh
./check_file.sh
```

#### 18. 순환문
```
nano batch_job.sh

#!/bin/bash
for i in {1..5}
do
        echo "work file $i created...."
        touch "job_$i.data"
        sleep 0.5
done
ls *.data


-> nano 저장 후 나와서
chmod +x batch_job.sh
./batch_job.sh
```

### 19. 시스템 모니터링
```
nano system_monitor.sh
---------------------------------> nano 진입

#!/bin/bash

# Server Name
server_name=$(hostname)

# Color variables for better readability (Industry standard practice for CLI tools)
GREEN='\033[0;32m'
BLUE='\033[0;34m'
RED='\033[0;31m'
CLEAR='\033[0m'

# Helper function for colored output
function print_color() {
    local color=$1
    local text=$2
    echo -e "${color}${text}${CLEAR}"
}

# --- Monitor Functions ---

function memory_check() {
    echo ""
    print_color "$BLUE" "Memory usage on ${server_name}:"
    free -h
    echo ""
}

function cpu_check() {
    echo ""
    print_color "$BLUE" "CPU load on ${server_name}:"
    uptime
    echo ""
}

function disk_check() {
    echo ""
    print_color "$BLUE" "Disk usage on ${server_name}:"
    df -h | grep -vE '^Filesystem|tmpfs|cdrom' # Exclude tmpfs and cdrom for cleaner output
    echo ""
}

function tcp_check() {
    echo ""
    print_color "$BLUE" "TCP connections count:"
    # In production, simple connection counting is common for quick health checks
    cat /proc/net/tcp | wc -l
    echo ""
}

function kernel_check() {
    echo ""
    print_color "$BLUE" "Kernel version:"
    uname -r
    echo ""
}

# --- Interactive Menu ---

function show_menu() {
    echo -e "\n${GREEN}--- System Monitor Menu ---${CLEAR}"
    echo "1) Check Memory Usage"
    echo "2) Check CPU Load"
    echo "3) Check Disk Usage"
    echo "4) Check TCP Connections"
    echo "5) Check Kernel Version"
    echo "6) Run All Checks"
    echo "0) Exit"
    echo -n "Choose an option: "
}

function main() {
    while true; do
        show_menu
        read -r option
        case $option in
            1) memory_check ;;
            2) cpu_check ;;
            3) disk_check ;;
            4) tcp_check ;;
            5) kernel_check ;;
            6) 
                memory_check
                cpu_check
                disk_check
                tcp_check
                kernel_check
                ;;
            0) 
                print_color "$GREEN" "Exiting..."
                exit 0 
                ;;
            *) 
                print_color "$RED" "Invalid option. Please try again."
                ;;
        esac
        
        echo -n "Press Enter to continue..."
        read -r
    done
}

# Start the script
main

----------------------> nano 저장 후 나와서
chmod +x system_monitor.sh
./system_monitor.sh 
```